/**
 * Re-export the shared architecture graph schema for extractor imports.
 *
 * Audit note: the actual schema now lives in `tools/graph-schema/src/index.ts`.
 */
export type * from "../../graph-schema/src/index.js";
